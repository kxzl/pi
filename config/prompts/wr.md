---
description: "End-to-end: implement, test, commit, and push"
argument-hint: "<task description>"
---

Complete this task end-to-end:

**Task:** $@

**Workflow:**
1. **Plan** — Read relevant code, state your approach in 3-5 bullet points.
2. **Implement** — Make the changes. Keep diffs minimal and focused.
3. **Test** — Run existing tests. If no tests exist, verify manually via bash.
4. **Commit** — Use conventional commit format (feat:, fix:, refactor:, etc.). Write a clear message.
5. **Push** — Push to the current branch.

At each step, show what you're doing and pause if anything unexpected happens.
